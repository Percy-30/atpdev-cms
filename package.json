{
  "name": "atpdev-monorepo",
  "private": true,
  "packageManager": "npm@11.6.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/portal && npm run build --workspace=apps/admin",
    "build:portal": "npm run build --workspace=apps/portal",
    "build:admin": "npm run build --workspace=apps/admin",
    "dev": "turbo run dev",
    "dev:portal": "npm run dev --workspace=apps/portal -- -p 3000",
    "dev:admin": "npm run dev --workspace=apps/admin -- -p 3001",
    "lint": "npm run lint --workspace=apps/portal && npm run lint --workspace=apps/admin",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "lightningcss": "^1.33.0",
    "prettier": "^3.2.5",
    "turbo": "^2.10.8"
  }
}
